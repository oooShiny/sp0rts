<div{{ attributes.addClass(['tw-flex tw-text-gray-200']) }}>
    {% for item in items %}
        <div{{ content_attributes.addClass(['layout-column']) }}>
          {{ item }}
        </div>
    {% endfor %}
</div>
